<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.example.ericm_000.popularmovies.MainFragment">

    <!-- TODO emorand why would I import this in the fragment ? -->
    <include android:id="@+id/toolbar_main" layout="@layout/toolbar" />

    <GridView
        android:id="@+id/gridview_movies"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:numColumns="3"
        android:verticalSpacing="0dp"
        android:horizontalSpacing="0dp"
        android:gravity="center"
        android:stretchMode="columnWidth"
        />

    <!--
                    android:columnWidth="@dimen/gridview_poster_width"
    -->

</LinearLayout>
